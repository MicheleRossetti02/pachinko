// Legacy file stub: the frontend is now implemented under `src/` and built with Vite.
// This file is intentionally a harmless ES module placeholder to avoid accidental
// inclusion of a UMD bundle. If you need the old UMD bundle, retrieve it from
// your git history or from the project backups.

// No runtime behavior here. Use `npm run dev` inside `pachinko-frontend` and
// open http://127.0.0.1:5173/ to view the app. The real app entrypoint is
// `pachinko-frontend/src/main.jsx`.

export default null;
    if (l.includes('nachos')) return '🌽';

    if (l.includes('niente') || l.includes('no')) return '▫️';
